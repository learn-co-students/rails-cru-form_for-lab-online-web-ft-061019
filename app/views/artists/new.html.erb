<h3> New Artist</h3>

<%= form_for @artist do |a| %>
    <%= a.label :name, "Artist Name:" %>
    <%= a.text_field :name %><br>
    
    <%= a.label :bio, "Artist Bio:" %>
    <%= a.text_area :bio %><br>

    <%= a.submit %>
<% end %>